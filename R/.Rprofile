# Nitesh Mishra
options(tidyverse.quiet = TRUE)
options(scipen = 999)
options(highcharter.rjson = TRUE)

ggplot2::theme_set(
    ggthemes::theme_few()
)

ggplot2::update_geom_defaults(
    "point",
    list(
        size = 3,
        colour = "gray20",
        fill = "#E96245",
        shape = 21,
        stroke = 0.75,
        alpha = 0.8
    )
)

ggplot2::update_geom_defaults(
    "line",
    list(
        colour = "gray20",
        linetype = "dashed",
        lineend = "butt",
        arrow = NULL,
        linewidth = 0.35
    )
)

ggplot2::update_geom_defaults(
    "boxplot",
    list(
        colour = "gray10",
        fill = "gray90",
        linewidth = 0.25,
        outlier.shape = 21,
        alpha = 0.6
    )
)
